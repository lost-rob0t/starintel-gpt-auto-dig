name: Worker 7 materialize issue 1903 packet

on:
  push:
    branches:
      - autodig/worker-7-issue-1903-syracuse-axon-policy
    paths:
      - ".github/workflows/worker7-materialize-1903.yml"

permissions:
  contents: write

concurrency:
  group: worker7-materialize-1903
  cancel-in-progress: false

jobs:
  materialize:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out exact branch head
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Import packet transactionally
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/starintel.py import \
            digs/flock/2026-09-15-request-1903-syracuse-axon-policy/starintel-documents.jsonl

      - name: Validate complete normalized corpus
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/starintel.py validate --root .

      - name: Commit normalized DB surfaces and remove one-shot workflow
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/worker7-materialize-1903.yml
          git add -A db/
          if git diff --cached --quiet; then
            echo "Importer produced no repository delta." >&2
            exit 2
          fi
          git commit -m "Materialize issue 1903 StarIntel records"
          git push origin HEAD:autodig/worker-7-issue-1903-syracuse-axon-policy
