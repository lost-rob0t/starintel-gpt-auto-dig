name: Worker 8 materialize request 1904

on:
  push:
    branches:
      - agent/worker8-tompkins-notice-deadline-1904-20260915
    paths:
      - digs/flock/2026-09-15-request-1904-notice-deadline/starintel-documents.jsonl
      - .github/workflows/worker8-materialize-1904.yml

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Import packet through canonical writer
        run: python3 scripts/starintel.py import digs/flock/2026-09-15-request-1904-notice-deadline/starintel-documents.jsonl

      - name: Commit importer-generated DB records
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add db/
          if git diff --cached --quiet; then
            echo "No importer-generated DB changes"
          else
            git commit -m "Materialize #1904 canonical records"
            git push origin HEAD:${GITHUB_REF_NAME}
          fi

      - name: Validate complete StarIntel corpus
        run: python3 scripts/starintel.py validate --root .
