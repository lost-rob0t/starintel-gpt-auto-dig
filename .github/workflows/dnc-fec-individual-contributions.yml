name: Generate DNC FEC corpus (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'The monolithic importer is retired.'
          echo 'Use materialize_dnc_fec_people_index.py and its sharded workflow.'
