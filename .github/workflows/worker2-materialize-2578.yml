name: Worker 2 materialize issue 2578 packet

on:
  push:
    branches:
      - autodig/worker-2-issue-2578-dallas-caption-resolution
    paths:
      - ".github/workflows/worker2-materialize-2578.yml"

permissions:
  contents: write

concurrency:
  group: worker2-materialize-2578
  cancel-in-progress: false

jobs:
  materialize:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out exact branch head
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Import packet transactionally
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/starintel.py import \
            digs/dallas/2026-09-15-forward-observer-source-caption-resolution/starintel-documents.jsonl

      - name: Validate complete normalized corpus
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/starintel.py validate --root .

      - name: Commit normalized DB surfaces and remove one-shot workflow
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/worker2-materialize-2578.yml
          git add -A db/
          if git diff --cached --quiet; then
            echo "Importer produced no repository delta." >&2
            exit 2
          fi
          git commit -m "Materialize issue 2578 StarIntel records"
          git push origin HEAD:autodig/worker-2-issue-2578-dallas-caption-resolution
