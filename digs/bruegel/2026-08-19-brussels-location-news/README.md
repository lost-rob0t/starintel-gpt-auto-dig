# Bruegel Brussels office semantics and current-news scan

This hourly Auto-Dig pass used the repository's global dataset-first selector against the complete **54-entry** current dataset/topic universe. Current main exposed **37 normalized investigation-target records** in the hourly frontier audit. The run seed is `16648165146820115613`. The previous three successful hourly datasets, `rockefeller-foundation-public-roster`, `usaid-doge-mortality-accountability`, and `chatham-house-public-roster`, were hard-excluded together with avoidable repeats of their dominant New York, Washington, DC, and London geographies.

Deterministic redraws rejected direct active overlap, repeated geography, recently used diversity surfaces, meta/non-HQ surfaces, or candidates without a coherent organization-location target. The first acceptable dataset/topic was **`bruegel-public-roster`**. Its canonical target pool contains `starintel:org:bruegel`, selected with target seed `14936729465846923484`. The complete candidate pool and rejection trace are persisted in the `research-pass`.

## Location result

Bruegel's first-party event pages repeatedly publish **Rue de la Charité 33, 4th floor, 1210 Brussels** as the venue for Bruegel-hosted on-site activity. That evidence supports a public operational office/event venue. It does **not** explicitly establish headquarters or a legal/statutory seat, so this packet records `primary_office` / `operates_primary_office_at` semantics and queues authoritative HQ/legal-seat resolution. No coordinates are inferred.

## Current-news result

Only after the location classification was bounded, current Bruegel news, publication, event, leadership, and location searches were reviewed through **2026-08-19**. The fresh surfaces found research/output updates but no material organizational, leadership, relationship, or location change that justified a new event or claim. No filler record was added.

## Records

Six typed v0.9.0 records are added: two `source`, one `location`, one `relation`, one `investigation-target`, and one `research-pass`. The canonical record dataset remains **`bruegel`**; no new dataset, schema field, or enrichment pipeline is introduced.
